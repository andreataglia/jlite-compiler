//Source: Pathier vectors.jlite
class Main {
  Void main(Int argc, String argv){
    /*
     Main function
     Performs some calculations
    */
    String currentChar; // Unused
    Point p1;
    Point p2;
    Int buffer;
    Vector v;

    v = new Vector();
    v.p1 = new Point();
    p2 = new Point();
    v.getPoint().x = 90;
    v.p1.y = 89;
    p2.x = 43 + 78 * 9 - 7;
    readln(buffer);
    p2.y = buffer;


    println(v.norm() * 7 + 45 * 9);

    return;
  }
}

class Point {
  Int x;
  Int y;

  String toString() {
    return "Point (x = \" \n \r + this.y + )";
  }
}

class Vector {
  Point p1;
  Point p2;
  String s;
  Bool b;
  Int i;

  Int norm() {
    Int res;
    res = (p1.x - p2.x) * (p1.x - p2.x) + (p2.y - p1.y) * (p2.y - p1.y);
    b = true;
    res = should_be_float("ee", true, "s", 9000);
    return res + 67 - 67;
  }

  Int should_be_float(String a, Bool b, String c, Int d){
      return d + d;
  }

  Point getPoint(){
      return new Point();
  }
}
